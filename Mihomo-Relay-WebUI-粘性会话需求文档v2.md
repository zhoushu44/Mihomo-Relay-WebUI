# Mihomo-Relay-WebUI 粘性会话需求文档 v2.0

> 文档日期：2026-08-16
> 基于v1.0需求文档 + 用户确认的全部功能决策

---

## 一、架构方案

### 1.1 粘性实现方式

**端口映射 + mihomo listener 热重载**

- 每个任务 acquire 时，系统在 mihomo 配置中动态添加一个 listener 端口，绑定一个固定代理/节点
- 端口范围 40001-40999，自动寻找空闲端口分配
- mihomo 热重载配置（不重启容器，不断连）
- release 时移除该 listener，释放端口
- **所有粘性端口必须带认证才能使用**（跟现有7890/7891一样的账号密码），扫到端口没密码连不上

### 1.2 端口使用

| 端口 | 协议 | 用途 | 认证 | 开放范围 |
|------|------|------|------|----------|
| 7890 | TCP | SOCKS5 入口 | 账号密码 | 公网开放 |
| 7891 | TCP | HTTP 入口 | 账号密码 | 公网开放 |
| 7892 | TCP | WebUI + API | API Key | 公网开放 |
| 40001-40999 | TCP | 粘性会话动态端口 | 账号密码 | 公网开放（仅粘性模式） |

- 粘性端口复用现有 SOCKS5/HTTP 账号密码认证
- 脚本连接粘性端口时带账号密码：`socks5://用户名:密码@服务器IP:40001`
- 无密码连接被拒绝

### 1.2 数据存储

- **纯内存**，不持久化
- 服务器重启后所有会话清空，重新开始

### 1.3 模式切换

- WebUI 总开关：粘性会话模式 vs 现有轮换模式
- 开启粘性模式后，acquire/release API 生效
- 关闭后回退到现有 round-robin 轮换

---

## 二、全局配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 粘性模式开关 | 关闭 | 总开关，开启后进入粘性会话模式 |
| 会话超时 | 600s（10分钟） | 会话超时自动释放代理槽位 |
| 排队超时 | 30s | IP不足时排队等待，超过30秒返回失败 |
| 端口范围 | 40001-40999 | 动态端口自动分配范围 |
| 测试URL | `https://ip-api.com/json` | 代理可用性验证URL，WebUI可配置 |
| 测试URL开关 | 开启 | 可关闭代理验证功能 |

---

## 三、各场景粘性行为

### 3.1 场景 A：直连

- **不需要粘性**
- 直连本身就是固定服务器IP，脚本直接用 7890/7891 即可

### 3.2 场景 B/C/D：手填代理列表

| 项目 | 说明 |
|------|------|
| 粘性支持 | 支持 |
| 代理有效期 | 永久（静态代理） |
| 并发策略 | **不限制并发**，每个任务都能拿到代理，同一个IP可同时给多个任务用 |
| 分配策略 | 轮询分配（round-robin） |
| 故障切换 | 绑定后代理挂了，自动切换到下一个可用代理，**端口不变** |
| 健康检查 | 定时检查（每60秒）+ 连续失败3次自动剔除 |
| release后 | 代理归还池中，可继续分配给其他任务 |

**场景B/C/D故障切换流程**：

```
会话进行中，后台每60秒检测代理
    │
    ├── 代理可用 → 继续使用，端口不变
    └── 代理不可用 → 失败计数+1
        ├── 失败<3次 → 自动切换到下一个可用代理，端口不变
        └── 失败≥3次 → 代理从池中剔除，切换到下一个可用代理，端口不变
```

### 3.3 场景 E：API 提取

| 项目 | 说明 |
|------|------|
| 粘性支持 | 支持 |
| 提取时机 | **懒加载**，acquire时才调API提取，不预提取 |
| 提取数量 | 每次1个 |
| 代理独享 | **1任务1IP**，每个任务独立提取一个新代理，不复用已提取的 |
| 代理有效期 | 10分钟（API平台固定） |
| 代理格式 | 完整URL自动解析（如 `socks5://ip:port`） |
| 提取失败 | 自动重试3次，都失败才返回错误 |
| 提取后验证 | 请求测试URL验证可用性（可关闭），不可用则重试提取新的 |
| 过期处理 | 10分钟到期自动释放+清理，任务没完成就算了 |
| 故障处理 | **失败就失败，不自动切换**，脚本需重新acquire |
| release后 | 释放即移除（不归还池中） |
| 并发提取 | 不限制，多个任务可同时调API提取 |
| 白名单 | 需在上游代理平台手动加服务器IP白名单 |
| 提取限流 | 不限流 |

**场景E流程**：

```
acquire(task_id)
    │
    ├── 调API提取1个新代理（重试3次）
    │   ├── 提取成功 → 验证可用性（可关闭）
    │   │   ├── 可用 → 分配端口+绑定代理 → 返回
    │   │   └── 不可用 → 重新提取（重试3次内）
    │   └── 提取失败3次 → 返回错误
    │
    └── 10分钟到期 → 自动释放+清理

release(task_id)
    └── 移除代理+移除listener端口
```

### 3.4 场景 F：Clash 订阅

**两个模式，F卡片内切换**：

| 模式 | 说明 |
|------|------|
| 直连模式 | 粘性：绑定第一个可用节点，不过期，故障自动切换 |
| 轮询模式 | 粘性：每个任务轮询分配一个节点，10分钟过期 |

**直连模式详情**：

| 项目 | 说明 |
|------|------|
| 粘性支持 | 支持 |
| 节点有效期 | **不过期**，只有故障才换 |
| 节点选择 | acquire时选第一个可用节点 |
| 故障切换 | 节点挂了自动切换到下一个可用节点，**端口不变** |
| 故障检测 | acquire时检测 + 后台每60秒定时检测 |
| 失败计数 | 共享失败计数，连续失败3次标记为"不可用"，不再尝试 |
| 订阅更新 | 每10分钟自动重新拉取订阅，重置失败计数 |
| 全部不可用 | 遍历所有节点都不可用 → 返回失败 |
| 切换次数 | 不限次数，遍历所有可用节点直到找到可用的 |
| release后 | 节点归还池中，可继续分配 |

**场景F直连模式流程**：

```
acquire(task_id)
    │
    ├── 从订阅节点列表中选第一个可用节点
    │   ├── 跳过已标记不可用的节点（失败3次的）
    │   ├── 检测节点可用性（请求测试URL）
    │   │   ├── 可用 → 分配端口+绑定节点 → 返回
    │   │   └── 不可用 → 失败计数+1，试下一个节点
    │   └── 所有节点都不可用 → 返回失败
    │
    └── 会话进行中，后台每60秒检测
        ├── 节点可用 → 继续使用，端口不变
        └── 节点不可用 → 自动切换到下一个可用节点，端口不变

release(task_id)
    └── 节点归还池中+移除listener端口
```

**轮询模式详情**：

| 项目 | 说明 |
|------|------|
| 代理有效期 | 10分钟过期 |
| 分配策略 | 轮询分配+不可用跳过+复用 |
| 节点不够 | 复用已分配的节点 |
| 不可用节点 | acquire时检测，不可用跳过试下一个 |
| 节点记忆 | 连续失败3次标记为"不可用"，不再尝试，订阅更新时重置 |
| 节点验证 | 请求测试URL验证（跟E一样，可关闭） |
| 订阅更新 | 每10分钟自动重新拉取订阅 |
| release后 | 节点归还池中，可继续分配 |

**场景F轮询模式流程**：

```
acquire(task_id)
    │
    ├── 从订阅节点列表中轮询选取
    │   ├── 跳过已标记不可用的节点（失败3次的）
    │   ├── 检测节点可用性（请求测试URL）
    │   │   ├── 可用 → 分配端口+绑定节点 → 返回
    │   │   └── 不可用 → 失败计数+1，跳过下一个
    │   └── 所有节点都占用 → 复用已分配的节点
    │
    └── 10分钟到期 → 自动释放+清理

release(task_id)
    └── 节点归还池中+移除listener端口
```

---

## 四、API 接口（商用级规范化）

### 4.1 认证方式

- URL参数：`?key=API_KEY`
- 请求头：`X-API-Key: API_KEY`

### 4.2 接口列表

| 接口 | 方法 | 功能 |
|------|------|------|
| `/api/session/acquire` | POST | 获取粘性会话（分配端口+绑定代理） |
| `/api/session/release` | POST | 释放会话（移除端口+归还代理） |
| `/api/session/status` | GET | 查询某个会话状态 |
| `/api/session/list` | GET | 列出所有活跃会话 |
| `/api/pool/status` | GET | 查看代理池状态 |

### 4.3 接口详情

#### POST /api/session/acquire

获取一个粘性会话，系统自动分配端口+绑定代理。

```bash
POST /api/session/acquire?key=API_KEY
Content-Type: application/json

{"task_id": "register_001"}
```

**成功响应**：

```json
{
  "ok": true,
  "session": {
    "task_id": "register_001",
    "proxy": "1.2.3.4:1080",
    "proxy_type": "socks5",
    "listener": "服务器IP:40001",
    "listener_port": 40001,
    "acquired_at": "2026-08-16T10:00:00Z",
    "expires_at": "2026-08-16T10:10:00Z"
  }
}
```

**失败响应**：

```json
{
  "ok": false,
  "error": "代理提取失败，已重试3次"
}
```

**排队响应**（30秒超时后）：

```json
{
  "ok": false,
  "error": "排队超时，无可用代理"
}
```

#### POST /api/session/release

释放会话，移除listener端口，归还/移除代理。

```bash
POST /api/session/release?key=API_KEY
Content-Type: application/json

{"task_id": "register_001"}
```

```json
{
  "ok": true,
  "message": "会话已释放"
}
```

#### GET /api/session/status

查询某个会话状态。

```bash
GET /api/session/status?key=API_KEY&task_id=register_001
```

```json
{
  "ok": true,
  "session": {
    "task_id": "register_001",
    "proxy": "1.2.3.4:1080",
    "listener": "服务器IP:40001",
    "acquired_at": "2026-08-16T10:00:00Z",
    "expires_at": "2026-08-16T10:10:00Z",
    "status": "active"
  }
}
```

#### GET /api/session/list

列出所有活跃会话。

```bash
GET /api/session/list?key=API_KEY
```

```json
{
  "ok": true,
  "sessions": [
    {
      "task_id": "register_001",
      "proxy": "1.2.3.4:1080",
      "listener": "服务器IP:40001",
      "acquired_at": "2026-08-16T10:00:00Z",
      "expires_at": "2026-08-16T10:10:00Z"
    }
  ],
  "total": 1
}
```

#### GET /api/pool/status

查看代理池状态。

```bash
GET /api/pool/status?key=API_KEY
```

```json
{
  "ok": true,
  "pool": {
    "total": 10,
    "available": 3,
    "in_use": 7,
    "queued": 0,
    "proxies": [
      {"proxy": "1.2.3.4:1080", "in_use": 1, "status": "busy", "health": "healthy"},
      {"proxy": "5.6.7.8:1080", "in_use": 0, "status": "available", "health": "healthy"}
    ]
  }
}
```

---

## 五、WebUI 界面

### 5.1 粘性会话管理面板

| UI元素 | 说明 |
|--------|------|
| 总开关 | 启用/关闭粘性会话模式 |
| 测试URL输入框 | 可配置测试URL（默认 `https://ip-api.com/json`） |
| 测试URL开关 | 可关闭代理验证功能 |
| 代理池状态 | 显示总数/可用/占用/排队 |
| 活跃会话列表 | 表格显示 task_id、绑定代理、端口、获取时间、超时时间 |
| 手动释放按钮 | 可手动释放某个卡住的会话 |

### 5.2 场景F卡片变更

F卡片内新增模式切换：
- **直连模式**：粘性，绑定第一个可用节点，不过期，故障自动切换（端口不变）
- **轮询模式**：粘性，每个任务轮询分配节点，10分钟过期

---

## 六、技术实现

### 6.1 核心模块

| 模块 | 说明 |
|------|------|
| session_store | 内存字典 `{task_id → session_info}`，线程安全 |
| proxy_pool | 内存字典 `{proxy_key → proxy_info}`，线程安全 |
| 端口分配器 | 40001-40999范围内自动分配/释放空闲端口 |
| mihomo配置生成 | 动态生成含多个listener的mihomo配置 |
| mihomo热重载 | 通过Docker API或信号热重载配置，不重启容器 |
| 后台清理线程 | 定时扫描过期会话和代理，自动清理 |
| 日志系统 | logger.info记录会话acquire/release/提取/过期全流程 |

### 6.2 线程安全

- `threading.Lock` 保护 session_store 和 proxy_pool
- 所有读写操作都在锁内

### 6.3 mihomo 部署方式

**mihomo 容器内置运行，不重新部署**

- mihomo 容器（`metacubex/mihomo:latest`）启动后一直运行
- 切换场景：只更新配置文件 + 热重载，不删除重建容器
- acquire/release：只增删 listener 配置 + 热重载
- 热重载方式：更新挂载的配置文件 → 发送 SIGHUP 信号 或 调用 mihomo API 重载

### 6.4 mihomo listener 热重载

```yaml
# 每个粘性会话生成一个listener
listeners:
  - name: sticky-40001
    type: socks
    port: 40001
    proxy: "代理节点名称"  # 绑定固定代理
  - name: sticky-40002
    type: socks
    port: 40002
    proxy: "另一个代理节点名称"
```

acquire 时添加 listener，release 时移除，然后热重载配置。

---

## 七、日志

所有核心操作记录 logger.info：

| 日志点 | 说明 |
|--------|------|
| acquire请求 | task_id、场景、池大小 |
| 命中已有会话 | task_id、proxy、port |
| 提取代理（E） | API调用、返回结果、验证结果 |
| 分配节点（F） | 选取节点、检测结果、失败计数 |
| 分配成功 | task_id、proxy、port、过期时间 |
| 排队等待 | task_id、队列位置 |
| release请求 | task_id、释放的proxy/port |
| 过期清理 | task_id、proxy、过期原因 |
| 健康检查 | 代理状态变化、剔除事件 |
| 热重载 | 配置更新、listener增删 |

---

## 八、验收标准

| 编号 | 验收项 |
|------|--------|
| A1 | 总开关开启粘性模式，acquire/release正常工作 |
| A2 | 场景B/C/D：多个任务轮询分配代理，同一个IP可同时给多个任务用 |
| A3 | 场景E：acquire时懒加载提取代理，10分钟到期自动清理 |
| A4 | 场景E：提取失败重试3次后返回错误 |
| A5 | 场景E：提取后验证代理可用性（可关闭） |
| A6 | 场景F：直连/轮询模式切换正常，两个模式都支持粘性 |
| A7 | 场景F直连：绑定第一个可用节点，不过期，故障自动切换（端口不变） |
| A8 | 场景F轮询：每个任务绑定一个节点，不可用跳过，10分钟过期 |
| A9 | 场景F：连续失败3次的节点标记不可用，不再尝试 |
| A10 | 场景F直连：遍历所有节点都不可用时返回失败 |
| A11 | 场景F轮询：节点不够时复用已分配节点 |
| A12 | 场景B/C/D：绑定后代理故障自动切换，端口不变 |
| A13 | mihomo listener热重载正常，acquire/release不重启容器 |
| A14 | 场景E：10分钟过期自动释放+清理，失败不自动切换 |
| A15 | 会话10分钟超时自动释放（场景E/F轮询） |
| A16 | 排队30秒超时返回失败 |
| A17 | /api/session/acquire、release、status、list、pool/status正常工作 |
| A18 | WebUI显示代理池状态、活跃会话列表、手动释放 |
| A19 | WebUI测试URL输入框+开关可配置 |
| A20 | 关闭粘性模式后，现有场景A~F功能不受影响 |
